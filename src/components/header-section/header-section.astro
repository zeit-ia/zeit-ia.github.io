---
import NavBar from "../NavBar.astro";
import PillButton from "../PillButton.astro";
import headerBg from "../../shared-images/header-bg.png";
import { getImage } from "astro:assets";

const optimizedBackgroundImage = await getImage({ src: headerBg });
---

<section
  id="home"
  class="relative flex min-h-screen flex-col bg-black bg-cover bg-center bg-no-repeat"
  style={`background-image: url(${optimizedBackgroundImage.src});
    background-color: rgb(0, 0, 0, 0.50);
    background-blend-mode: darken;
    `}
>
  <!-- Glass Navigation Bar -->
  <NavBar />

  <!-- Main Content -->
  <div
    class="mx-6 flex h-full max-w-5xl flex-1 flex-col justify-evenly justify-self-center md:mx-20 xl:mx-28"
  >
    <h1
      class="text-brand-secondary mb-8 text-left font-mono text-5xl leading-tight font-light md:text-5xl"
    >
      O futuro da sua produção começa com uma decisão agora.
    </h1>

    <p
      class="text-textlight mb-12 text-left text-base leading-relaxed opacity-90"
    >
      Na Zeit, entendemos que tempo e precisão são decisivos no campo. Por isso,
      desenvolvemos soluções que otimizam análises e trazem mais controle e
      rentabilidade para a safra.
    </p>

    <div class="flex">
      <a href="#solutions">
        <PillButton>ENCONTRE A SOLUÇÃO IDEAL PARA SUA LAVOURA</PillButton>
      </a>
    </div>
  </div>

  <!-- Bottom Section -->
  <a href="#presentation">
    <div class="relative z-10 flex flex-col items-center pb-8">
      <p class="mb-4 text-sm font-medium tracking-wide text-white">
        CONHEÇA NOSSA HISTÓRIA
      </p>
      <div class="animate-bounce">
        <svg
          width="49"
          height="48"
          viewBox="0 0 49 48"
          fill="none"
          xmlns="http://www.w3.org/2000/svg"
        >
          <path
            d="M14.4082 26L24.4082 36L34.4082 26"
            stroke="#F0EDEE"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"></path>
          <path
            d="M14.4082 12L24.4082 22L34.4082 12"
            stroke="#F0EDEE"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"></path>
        </svg>
      </div>
    </div>
  </a>
</section>

<style>
  @keyframes bounce {
    0%,
    20%,
    53%,
    80%,
    100% {
      transform: translateY(0);
    }
    40%,
    43% {
      transform: translateY(-10px);
    }
    70% {
      transform: translateY(-3px);
    }
  }

  .animate-bounce {
    animation: bounce 2s infinite;
  }
</style>
